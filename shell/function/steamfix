# vim:ft=sh
function steamfix() {
	require steam find || return 1
	find ~/.steam/root/ ( -name 'libgcc_s.so*' -o -name 'libstdc++.so*' -o -name 'libxcb.so*' -o -name 'libgpg-error.so*' ) -print -delete
}
